services:
  app:
    image: myorg/app:1.0
    profiles: ["prod", "staging"]
    deploy:
      placement:
        constraints:
          - "node.role==worker"
        preferences:
          - spread: node.labels.zone

x-security-defaults: &security-defaults
  sysctls:
    - net.ipv6.conf.all.disable_ipv6=1
  cap_drop:
    - AUDIT_CONTROL
    - AUDIT_READ
    - AUDIT_WRITE
    - BLOCK_SUSPEND
    - BPF
    - CHECKPOINT_RESTORE
    - DAC_READ_SEARCH
    - FSETID
    - IPC_LOCK
    - IPC_OWNER
    - KILL
    - LEASE
    - LINUX_IMMUTABLE
    - MAC_ADMIN
    - MAC_OVERRIDE
    - MKNOD
    - NET_ADMIN
    - NET_BROADCAST
    - PERFMON
    - SYS_BOOT
    - SYS_MODULE
    - SYS_NICE
    - SYS_PACCT
    - SYS_PTRACE
    - SYS_RAWIO
    - SYS_RESOURCE
    - SYS_TIME
    - SYS_TTY_CONFIG
    - SYSLOG
    - WAKE_ALARM
  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "5"

x-logging-defaults: &logging-defaults
  sysctls:
    - net.ipv6.conf.all.disable_ipv6=1
  logging:
    driver: "json-file"
    options:
      max-size: "10m"
      max-file: "5"

x-shared-env: &shared-env
  # --- Core ---
  SECRET_KEY: ${SECRET_KEY:-}
  LOG_LEVEL: ${LOG_LEVEL:-INFO}
  EDITION: SELF_HOSTED
  DEPLOY_ENV: ${DEPLOY_ENV:-PRODUCTION}
  CONSOLE_WEB_URL: ${CONSOLE_WEB_URL:-}
  CONSOLE_API_URL: ${CONSOLE_API_URL:-}
  SERVICE_API_URL: ${SERVICE_API_URL:-}
  APP_API_URL: ${APP_API_URL:-}
  APP_WEB_URL: ${APP_WEB_URL:-}
  FILES_URL: ${FILES_URL:-}
  FILES_ACCESS_TIMEOUT: ${FILES_ACCESS_TIMEOUT:-300}
  MIGRATION_ENABLED: ${MIGRATION_ENABLED:-true}
  # --- Init admin ---
  INIT_PASSWORD: ${INIT_PASSWORD:-}
  # --- Database ---
  DB_USERNAME: ${DB_USERNAME:-postgres}
  DB_PASSWORD: ${DB_PASSWORD:-}
  DB_HOST: ${DB_HOST:-db}
  DB_PORT: ${DB_PORT:-5432}
  DB_DATABASE: ${DB_DATABASE:-dify}
  SQLALCHEMY_POOL_SIZE: ${SQLALCHEMY_POOL_SIZE:-30}
  SQLALCHEMY_POOL_RECYCLE: ${SQLALCHEMY_POOL_RECYCLE:-3600}
  SQLALCHEMY_ECHO: ${SQLALCHEMY_ECHO:-false}
  # --- Redis ---
  REDIS_HOST: ${REDIS_HOST:-redis}
  REDIS_PORT: ${REDIS_PORT:-6379}
  REDIS_PASSWORD: ${REDIS_PASSWORD:-}
  REDIS_DB: ${REDIS_DB:-0}
  REDIS_USE_SSL: ${REDIS_USE_SSL:-false}
  CELERY_BROKER_URL: redis://:${REDIS_PASSWORD:-}@${REDIS_HOST:-redis}:${REDIS_PORT:-6379}/1
  BROKER_USE_SSL: ${BROKER_USE_SSL:-false}
  # --- Storage ---
  STORAGE_TYPE: ${STORAGE_TYPE:-local}
  STORAGE_LOCAL_PATH: /app/api/storage
  S3_ENDPOINT: ${S3_ENDPOINT:-}
  S3_BUCKET_NAME: ${S3_BUCKET_NAME:-dify-storage}
  S3_ACCESS_KEY: ${S3_ACCESS_KEY:-}
  S3_SECRET_KEY: ${S3_SECRET_KEY:-}
  S3_REGION: ${S3_REGION:-us-east-1}
  S3_USE_AWS_MANAGED_IAM: ${S3_USE_AWS_MANAGED_IAM:-false}
  # --- Vector Store ---
  VECTOR_STORE: ${VECTOR_STORE:-weaviate}
  WEAVIATE_ENDPOINT: ${WEAVIATE_ENDPOINT:-http://weaviate:8080}
  WEAVIATE_API_KEY: ${WEAVIATE_API_KEY:-}
  QDRANT_HOST: ${QDRANT_HOST:-qdrant}
  QDRANT_PORT: ${QDRANT_PORT:-6333}
  QDRANT_API_KEY: ${QDRANT_API_KEY:-}
  MILVUS_URI: ${MILVUS_URI:-http://milvus:19530}
  MILVUS_TOKEN: ${MILVUS_TOKEN:-}
  MILVUS_USER: ${MILVUS_USER:-root}
  MILVUS_PASSWORD: ${MILVUS_PASSWORD:-}
  MILVUS_DATABASE: ${MILVUS_DATABASE:-agmind}
  MILVUS_ENABLE_HYBRID_SEARCH: ${MILVUS_ENABLE_HYBRID_SEARCH:-true}
  MILVUS_ANALYZER_PARAMS: ${MILVUS_ANALYZER_PARAMS:-}
  # --- Mail (disabled) ---
  MAIL_TYPE: ${MAIL_TYPE:-}
  MAIL_DEFAULT_SEND_FROM: ${MAIL_DEFAULT_SEND_FROM:-}
  # --- SSRF ---
  SSRF_PROXY_HTTP_URL: ${SSRF_PROXY_HTTP_URL:-http://ssrf_proxy:3128}
  SSRF_PROXY_HTTPS_URL: ${SSRF_PROXY_HTTPS_URL:-http://ssrf_proxy:3128}
  SSRF_DEFAULT_READ_TIME_OUT: ${SSRF_DEFAULT_READ_TIME_OUT:-60}
  # --- Sandbox ---
  CODE_EXECUTION_ENDPOINT: ${CODE_EXECUTION_ENDPOINT:-http://sandbox:8194}
  CODE_EXECUTION_API_KEY: ${SANDBOX_API_KEY:-dify-sandbox}
  CODE_MAX_NUMBER: ${CODE_MAX_NUMBER:-9223372036854775807}
  CODE_MIN_NUMBER: ${CODE_MIN_NUMBER:--9223372036854775808}
  CODE_MAX_DEPTH: ${CODE_MAX_DEPTH:-5}
  CODE_MAX_PRECISION: ${CODE_MAX_PRECISION:-20}
  CODE_MAX_STRING_LENGTH: ${CODE_MAX_STRING_LENGTH:-80000}
  CODE_MAX_STRING_ARRAY_LENGTH: ${CODE_MAX_STRING_ARRAY_LENGTH:-30}
  CODE_MAX_OBJECT_ARRAY_LENGTH: ${CODE_MAX_OBJECT_ARRAY_LENGTH:-30}
  CODE_MAX_NUMBER_ARRAY_LENGTH: ${CODE_MAX_NUMBER_ARRAY_LENGTH:-1000}
  TEMPLATE_TRANSFORM_MAX_LENGTH: ${TEMPLATE_TRANSFORM_MAX_LENGTH:-80000}
  # --- Indexing ---
  INDEXING_MAX_SEGMENTATION_TOKENS_LENGTH: ${INDEXING_MAX_SEGMENTATION_TOKENS_LENGTH:-4000}
  # --- ETL ---
  ETL_TYPE: ${ETL_TYPE:-dify}
  UNSTRUCTURED_API_URL: ${UNSTRUCTURED_API_URL:-}
  # --- HTTP Request node limits (docling-serve returns large responses) ---
  HTTP_REQUEST_NODE_MAX_TEXT_SIZE: ${HTTP_REQUEST_NODE_MAX_TEXT_SIZE:-104857600}
  HTTP_REQUEST_NODE_MAX_BINARY_SIZE: ${HTTP_REQUEST_NODE_MAX_BINARY_SIZE:-104857600}
  # --- Marketplace ---
  MARKETPLACE_ENABLED: ${MARKETPLACE_ENABLED:-true}
  CHECK_UPDATE_URL: ${CHECK_UPDATE_URL:-https://updates.dify.ai}
  # Dify 1.14.1 defaults ENABLE_COLLABORATION_MODE=True → expects a separate websocket
  # service. Single-tenant LAN profile doesn't run that service — disable explicitly.
  ENABLE_COLLABORATION_MODE: ${ENABLE_COLLABORATION_MODE:-false}
  # --- Plugin Daemon ---
  PLUGIN_DAEMON_URL: ${PLUGIN_DAEMON_URL:-http://plugin_daemon:5002}
  PLUGIN_DAEMON_KEY: ${PLUGIN_DAEMON_KEY:-}
  PLUGIN_DAEMON_TIMEOUT: ${PLUGIN_DAEMON_TIMEOUT:-1800}
  INNER_API_KEY_FOR_PLUGIN: ${PLUGIN_INNER_API_KEY:-}
  # --- Ollama ---
  OLLAMA_API_BASE: ${OLLAMA_API_BASE:-http://ollama:11434}
  # --- Performance tuning (Celery / Graph engine) ---
  # Defaults = upstream Dify safe fallback для fresh installs без .env
  CELERY_WORKER_AMOUNT: ${CELERY_WORKER_AMOUNT:-1}
  CELERY_ACKS_LATE: ${CELERY_ACKS_LATE:-false}
  CELERY_WORKER_POOL: ${CELERY_WORKER_POOL:-gevent}
  # --- Upload limits (upstream defaults: 15MB / 5 files per batch — too small for KB ingest) ---
  UPLOAD_FILE_SIZE_LIMIT: ${UPLOAD_FILE_SIZE_LIMIT:-15}
  UPLOAD_FILE_BATCH_LIMIT: ${UPLOAD_FILE_BATCH_LIMIT:-5}
  BATCH_UPLOAD_LIMIT: ${BATCH_UPLOAD_LIMIT:-20}
  WORKFLOW_FILE_UPLOAD_LIMIT: ${WORKFLOW_FILE_UPLOAD_LIMIT:-10}

services:
  # ==================== Dify API ====================
  api:
    <<: *security-defaults
    image: langgenius/dify-api:${DIFY_VERSION:-1.14.1}
    container_name: agmind-api
    security_opt:
      - no-new-privileges:true
    restart: always
    mem_limit: ${API_MEM_LIMIT:-2g}
    environment:
      <<: *shared-env
      MODE: api
    volumes:
      - ./volumes/app/storage:/app/api/storage
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:5001/health 2>/dev/null || wget -qO- http://localhost:5001/health 2>/dev/null || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 60s
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      sandbox:
        condition: service_healthy
    networks:
      - agmind-backend
      - ssrf-network

  # ==================== Dify Worker ====================
  worker:
    <<: *security-defaults
    image: langgenius/dify-api:${DIFY_VERSION:-1.14.1}
    container_name: agmind-worker
    security_opt:
      - no-new-privileges:true
    restart: always
    mem_limit: ${WORKER_MEM_LIMIT:-2g}
    environment:
      <<: *shared-env
      MODE: worker
    volumes:
      - ./volumes/app/storage:/app/api/storage
    healthcheck:
      test: ["CMD-SHELL", "celery -A app.celery inspect ping -d celery@$$HOSTNAME 2>/dev/null || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 15s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 30s
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      sandbox:
        condition: service_healthy
    networks:
      - agmind-backend
      - ssrf-network

  # ==================== Dify Web (Console UI) ====================
  web:
    <<: *security-defaults
    image: langgenius/dify-web:${DIFY_VERSION:-1.14.1}
    container_name: agmind-web
    security_opt:
      - no-new-privileges:true
    restart: always
    mem_limit: ${WEB_MEM_LIMIT:-512m}
    depends_on:
      api:
        condition: service_healthy
    environment:
      CONSOLE_API_URL: ${CONSOLE_API_URL:-}
      APP_API_URL: ${APP_API_URL:-}
      SENTRY_DSN: ${WEB_SENTRY_DSN:-}
      NEXT_TELEMETRY_DISABLED: ${NEXT_TELEMETRY_DISABLED:-1}
      # Next.js binds to container hostname by default, not 0.0.0.0.
      # Without this, healthcheck on localhost:3000 gets Connection refused.
      HOSTNAME: "0.0.0.0"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3000 || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 15s
    networks:
      - agmind-backend

  # ==================== Open WebUI ====================
  # WARNING: Version pinned for white-label/re-branding compatibility.
  # Do NOT change to :main or :latest without verifying branding features still work.
  open-webui:
    <<: *security-defaults
    # WARNING: open-webui version pinned for white-label compatibility
    image: ghcr.io/open-webui/open-webui:${OPENWEBUI_VERSION:-v0.9.5}
    container_name: agmind-openwebui
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${OPENWEBUI_MEM_LIMIT:-2g}
    profiles:
      - openwebui
    environment:
      - WEBUI_NAME=AGMind
      - ENABLE_SIGNUP=${ENABLE_SIGNUP:-false}
      - ENABLE_LOGIN_FORM=true
      - DEFAULT_USER_ROLE=${DEFAULT_USER_ROLE:-user}
      - WEBUI_AUTH=true
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-}
      - ENABLE_OLLAMA_API=${ENABLE_OLLAMA_API:-false}
      - ENABLE_OPENAI_API=${ENABLE_OPENAI_API:-false}
      - OPENAI_API_BASE_URL=${OPENAI_API_BASE_URL:-}
      # Pipelines container removed 2026-04-26 — RAG лежит в Dify, OpenWebUI = только chat UI.
      # Disable HuggingFace model downloads — Open WebUI is UI-only proxy
      - ENABLE_RAG=false
      - RAG_EMBEDDING_MODEL=
      - USE_EMBEDDING_MODEL_DOCKER=
      - USE_RERANKING_MODEL_DOCKER=
      - USE_AUXILIARY_EMBEDDING_MODEL_DOCKER=
      - AUXILIARY_EMBEDDING_MODEL=
      - RAG_RERANKING_MODEL=
      - WHISPER_MODEL=
      - ENABLE_RAG_LOCAL_WEB_FETCH=false
    volumes:
      - agmind_openwebui_data:/app/backend/data
    healthcheck:
      test: ["CMD-SHELL", "unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY && curl -sf http://localhost:8080/health 2>/dev/null || wget -qO- http://localhost:8080/health 2>/dev/null || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 30s
    networks:
      - agmind-backend

  # ==================== Ollama ====================
  ollama:
    <<: *logging-defaults
    image: ollama/ollama:${OLLAMA_VERSION:-0.21.2}
    container_name: agmind-ollama
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${OLLAMA_MEM_LIMIT:-8g}
    profiles:
      - ollama
    # BUG-017: Go runtime has its own DNS resolver that ignores kernel sysctl.
    # GODEBUG=netdns=cgo forces Go to use the system (cgo) DNS resolver,
    # which respects sysctl disable_ipv6. Both are needed together.
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=1
    environment:
      - GODEBUG=netdns=cgo
    volumes:
      - agmind_ollama_data:/root/.ollama
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 30s
    #__GPU__deploy:
    #__GPU__  resources:
    #__GPU__    reservations:
    #__GPU__      devices:
    #__GPU__        - driver: nvidia
    #__GPU__          count: all
    #__GPU__          capabilities: [gpu]
    networks:
      - agmind-backend

  # ==================== vLLM (vllm profile) ====================
  # NOTE: На штатном AGmind (LLM_ON_PEER=true) этот блок НЕ активируется —
  # vLLM работает на peer Spark через docker-compose.worker.yml. Этот блок
  # для legacy single-node deploys или dev машин.
  # §8 dgx-spark defaults aligned with worker.yml: gpu_memory_utilization=0.60
  # (не 0.85 — на shared GPU 0.85 вызывает CUBLAS crash + docling OOM),
  # mem_limit=96g (не 16g — gemma-4 26B требует минимум 96g).
  vllm:
    <<: *logging-defaults
    image: ${VLLM_IMAGE:-vllm/vllm-openai:${VLLM_VERSION:-v0.19.0}${VLLM_CUDA_SUFFIX:-}}
    container_name: agmind-vllm
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${VLLM_MEM_LIMIT:-96g}
    mem_swappiness: 0
    profiles:
      - vllm
    environment:
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
      - CUDA_VISIBLE_DEVICES=${VLLM_CUDA_DEVICE:-0}
      # §8: NVML/libcuda требуют этот env. Без него torch.cuda.is_available=False.
      - NVIDIA_DRIVER_CAPABILITIES=${NVIDIA_DRIVER_CAPABILITIES:-compute,utility}
      # vLLM args — entrypoint wrapper reads these and builds argv as a bash array,
      # so JSON values (e.g. VLLM_SPECULATIVE_CONFIG) survive intact through compose
      # YAML rendering. See templates/vllm-config/entrypoint.sh + LANDMINES.md
      # "vLLM CLI: --speculative-config is JSON, not path".
      - VLLM_MODEL=${VLLM_MODEL:-QuantTrio/Qwen3.5-27B-AWQ}
      - VLLM_GPU_MEM_UTIL=${VLLM_GPU_MEM_UTIL:-0.60}
      - VLLM_MAX_MODEL_LEN=${VLLM_MAX_MODEL_LEN:-32768}
      - VLLM_CMD_PREFIX=${VLLM_CMD_PREFIX:-}
      - VLLM_EXTRA_ARGS=${VLLM_EXTRA_ARGS:-}
      - VLLM_SPECULATIVE_CONFIG=${VLLM_SPECULATIVE_CONFIG:-}
      - VLLM_ROPE_SCALING_CONFIG=${VLLM_ROPE_SCALING_CONFIG:-}
    entrypoint: ["/usr/local/bin/agmind-vllm-entrypoint.sh"]
    command: []
    volumes:
      - agmind_vllm_cache:/root/.cache/huggingface
      # vLLM argv wrapper — preserves JSON args through compose shlex.
      - ./vllm-config/entrypoint.sh:/usr/local/bin/agmind-vllm-entrypoint.sh:ro
    ipc: host
    ulimits:
      memlock:
        soft: -1
        hard: -1
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 1800s
    #__GPU__deploy:
    #__GPU__  resources:
    #__GPU__    reservations:
    #__GPU__      devices:
    #__GPU__        - driver: nvidia
    #__GPU__          device_ids: ['${VLLM_CUDA_DEVICE:-0}']
    #__GPU__          capabilities: [gpu]
    networks:
      - agmind-backend

  # ==================== TEI (tei profile) ====================
  tei:
    <<: *logging-defaults
    image: ghcr.io/huggingface/text-embeddings-inference:${TEI_EMBED_VERSION:-${TEI_VERSION:-cuda-1.9.3}}
    container_name: agmind-tei
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${TEI_MEM_LIMIT:-8g}
    profiles:
      - tei
    environment:
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
      - CUDA_VISIBLE_DEVICES=${TEI_CUDA_DEVICE:-0}
    command: --model-id ${EMBEDDING_MODEL:-deepvk/USER-bge-m3} --port 80
    volumes:
      - agmind_tei_cache:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:80/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 600s
    #__GPU__deploy:
    #__GPU__  resources:
    #__GPU__    reservations:
    #__GPU__      devices:
    #__GPU__        - driver: nvidia
    #__GPU__          device_ids: ['${TEI_CUDA_DEVICE:-0}']
    #__GPU__          capabilities: [gpu]
    networks:
      - agmind-backend

  # ==================== TEI Reranker (reranker profile, CPU) ================
  # Runs on CPU (ONNX backend) — cross-encoder models are incompatible with
  # TEI's CUDA/Candle backend (FlashBert). CPU is fine: reranker only
  # re-scores top 10-20 results, latency ~50-100ms per query.
  tei-rerank:
    <<: *logging-defaults
    image: ghcr.io/huggingface/text-embeddings-inference:${TEI_RERANK_VERSION:-cpu-1.9.3}
    container_name: agmind-tei-rerank
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${TEI_RERANK_MEM_LIMIT:-12g}
    profiles:
      - reranker
    environment:
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
      - CUDA_VISIBLE_DEVICES=${TEI_RERANK_CUDA_DEVICE:-0}
    command: --model-id ${RERANK_MODEL:-BAAI/bge-reranker-base} --port 80
    volumes:
      - agmind_tei_rerank_cache:/data
    #__GPU_RERANK__deploy:
    #__GPU_RERANK__  resources:
    #__GPU_RERANK__    reservations:
    #__GPU_RERANK__      devices:
    #__GPU_RERANK__        - driver: nvidia
    #__GPU_RERANK__          device_ids: ['${TEI_RERANK_CUDA_DEVICE:-0}']
    #__GPU_RERANK__          capabilities: [gpu]
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:80/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 600s
    networks:
      - agmind-backend

  # ==================== vLLM Embedding (DGX Spark / arm64) ==================
  vllm-embed:
    <<: *logging-defaults
    image: ${VLLM_EMBED_IMAGE:-nvcr.io/nvidia/vllm:${VLLM_NGC_VERSION:-26.02-py3}}
    container_name: agmind-vllm-embed
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${VLLM_EMBED_MEM_LIMIT:-3g}
    mem_swappiness: 0
    profiles:
      - vllm-embed
    # No depends_on chain: docker compose up -d would block waiting for vllm
    # to be healthy (vllm takes 30+ min to download 50 GB Gemma 4 on first run).
    # Reduced gpu_memory_utilization (embed=0.05, rerank=0.03) makes CUDA race
    # harmless — total parallel allocation ~10 GB stays in free memory.
    ipc: host
    ulimits:
      memlock:
        soft: -1
        hard: -1
    environment:
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
      - CUDA_VISIBLE_DEVICES=${VLLM_EMBED_CUDA_DEVICE:-0}
    command: vllm serve ${VLLM_EMBED_MODEL:-deepvk/USER-bge-m3} --runner pooling --host 0.0.0.0 --port 8000 --gpu-memory-utilization ${VLLM_EMBED_GPU_MEM_UTIL:-0.15} --trust-remote-code
    volumes:
      - agmind_vllm_embed_cache:/root/.cache/huggingface
    #__GPU__deploy:
    #__GPU__  resources:
    #__GPU__    reservations:
    #__GPU__      devices:
    #__GPU__        - driver: nvidia
    #__GPU__          count: all
    #__GPU__          capabilities: [gpu]
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 300s
    networks:
      - agmind-backend

  # ==================== vLLM Reranker (DGX Spark / arm64) ===================
  vllm-rerank:
    <<: *logging-defaults
    image: ${VLLM_RERANK_IMAGE:-nvcr.io/nvidia/vllm:${VLLM_NGC_VERSION:-26.02-py3}}
    container_name: agmind-vllm-rerank
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${VLLM_RERANK_MEM_LIMIT:-2g}
    mem_swappiness: 0
    profiles:
      - vllm-rerank
    # No depends_on: starts in parallel with vllm/vllm-embed. Safe after
    # gpu_memory_utilization reduction (see vllm-embed comment above).
    ipc: host
    ulimits:
      memlock:
        soft: -1
        hard: -1
    environment:
      - HUGGING_FACE_HUB_TOKEN=${HF_TOKEN:-}
      - CUDA_VISIBLE_DEVICES=${VLLM_RERANK_CUDA_DEVICE:-0}
    command: vllm serve ${VLLM_RERANK_MODEL:-BAAI/bge-reranker-v2-m3} --runner pooling --host 0.0.0.0 --port 8000 --gpu-memory-utilization ${VLLM_RERANK_GPU_MEM_UTIL:-0.10} --trust-remote-code
    volumes:
      - agmind_vllm_rerank_cache:/root/.cache/huggingface
    #__GPU__deploy:
    #__GPU__  resources:
    #__GPU__    reservations:
    #__GPU__      devices:
    #__GPU__        - driver: nvidia
    #__GPU__          count: all
    #__GPU__          capabilities: [gpu]
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 300s
    networks:
      - agmind-backend

  # ==================== PostgreSQL ====================
  db:
    <<: *security-defaults
    image: postgres:${POSTGRES_VERSION:-16-alpine3.23}
    container_name: agmind-db
    security_opt:
      - no-new-privileges:true
    restart: always
    mem_limit: ${DB_MEM_LIMIT:-4g}
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
      - FOWNER
    environment:
      PGUSER: ${DB_USERNAME:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-}
      POSTGRES_DB: ${DB_DATABASE:-dify}
      PGDATA: /var/lib/postgresql/data/pgdata
    command: >
      postgres -c 'max_connections=128'
               -c 'shared_buffers=4GB'
               -c 'work_mem=128MB'
               -c 'maintenance_work_mem=1GB'
               -c 'effective_cache_size=32GB'
               -c 'password_encryption=scram-sha-256'
               -c 'log_connections=on'
               -c 'log_disconnections=on'
               -c 'log_statement=ddl'
               -c 'log_min_duration_statement=1000'
    volumes:
      - ./volumes/db/data:/var/lib/postgresql/data
      - ../templates/init-dify-plugin-db.sql:/docker-entrypoint-initdb.d/01-create-plugin-db.sql:ro
      - ../templates/init-litellm-db.sql:/docker-entrypoint-initdb.d/02-create-litellm-db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - agmind-backend

  # ==================== Redis ====================
  redis:
    <<: *security-defaults
    image: redis:${REDIS_VERSION:-7.4.8-alpine}
    container_name: agmind-redis
    security_opt:
      - no-new-privileges:true
    restart: always
    mem_limit: ${REDIS_MEM_LIMIT:-1g}
    read_only: true
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
    tmpfs:
      - /tmp:size=64m
    command: redis-server /etc/redis/redis.conf
    volumes:
      - ./volumes/redis/data:/data
      - ./volumes/redis/redis.conf:/etc/redis/redis.conf:ro
    healthcheck:
      test: ["CMD-SHELL", "REDISCLI_AUTH='${REDIS_PASSWORD:-}' redis-cli ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - agmind-backend

  # ==================== Weaviate ====================
  weaviate:
    <<: *security-defaults
    image: semitechnologies/weaviate:${WEAVIATE_VERSION:-1.37.3}
    container_name: agmind-weaviate
    security_opt:
      - no-new-privileges:true
    restart: always
    mem_limit: ${WEAVIATE_MEM_LIMIT:-4g}
    profiles:
      - weaviate
    environment:
      QUERY_DEFAULTS_LIMIT: 25
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: "false"
      AUTHENTICATION_APIKEY_ENABLED: "true"
      AUTHENTICATION_APIKEY_ALLOWED_KEYS: ${WEAVIATE_API_KEY:-}
      AUTHENTICATION_APIKEY_USERS: hello@dify.ai
      AUTHORIZATION_ADMINLIST_ENABLED: "true"
      AUTHORIZATION_ADMINLIST_USERS: hello@dify.ai
      PERSISTENCE_DATA_PATH: /var/lib/weaviate
      DEFAULT_VECTORIZER_MODULE: none
      CLUSTER_HOSTNAME: node1
      GOMEMLIMIT: "3GiB"
      # v3.0 hotfix (2026-04-19): expose Prometheus /metrics on :2112,
      # required by prometheus.yml scrape job 'weaviate'. Was applied in runtime
      # (Phase 44) but missed from template — fresh install had target DOWN.
      PROMETHEUS_MONITORING_ENABLED: "true"
      PROMETHEUS_MONITORING_PORT: "2112"
    volumes:
      - ./volumes/weaviate:/var/lib/weaviate
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/v1/.well-known/ready 2>/dev/null || curl -sf http://localhost:8080/v1/.well-known/ready 2>/dev/null || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 15s
    networks:
      - agmind-backend

  # ==================== Qdrant (alternative vector store) ====================
  qdrant:
    <<: *security-defaults
    image: qdrant/qdrant:${QDRANT_VERSION:-v1.18.0}
    container_name: agmind-qdrant
    security_opt:
      - no-new-privileges:true
    restart: always
    mem_limit: ${QDRANT_MEM_LIMIT:-2g}
    profiles:
      - qdrant
    environment:
      QDRANT__SERVICE__API_KEY: ${QDRANT_API_KEY:-}
    volumes:
      - ./volumes/qdrant:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'echo > /dev/tcp/127.0.0.1/6333' || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 15s
    networks:
      - agmind-backend

  # ==================== Milvus (optional 3rd vector store, experimental) ====================
  # Selectable via wizard (3rd vector-DB option) or VECTOR_STORE=milvus env override.
  # build_compose_profiles auto-pulls the `minio` profile when VECTOR_STORE=milvus
  # (Milvus uses MinIO as S3 backend); wizard also auto-sets ENABLE_MINIO=true.
  # XOR with weaviate/qdrant — only one vector store active at a time.
  # EXPERIMENTAL: Dify-only. RAGFlow uses its own ES regardless of VECTOR_STORE
  # (PR #6367 closed not merged), so Milvus is irrelevant to RAGFlow KBs.
  # arm64 verified 2026-05-11 (milvusdb/milvus:v2.6.15, quay.io/coreos/etcd:v3.5.25).
  # Standalone mode: ~6-16 GiB RAM на 1-10M chunks. Use cases:
  # коллекции >50M chunks, multilingual built-in BM25 (CJK+RU), клиент уже на Milvus.
  milvus-etcd:
    <<: *security-defaults
    image: quay.io/coreos/etcd:${MILVUS_ETCD_VERSION:-v3.5.25}
    container_name: agmind-milvus-etcd
    security_opt:
      - no-new-privileges:true
    restart: always
    mem_limit: ${MILVUS_ETCD_MEM_LIMIT:-1g}
    profiles:
      - milvus
    environment:
      ETCD_AUTO_COMPACTION_MODE: revision
      ETCD_AUTO_COMPACTION_RETENTION: "1000"
      ETCD_QUOTA_BACKEND_BYTES: "4294967296"
      ETCD_SNAPSHOT_COUNT: "50000"
    command:
      - etcd
      - -advertise-client-urls=http://milvus-etcd:2379
      - -listen-client-urls=http://0.0.0.0:2379
      - --data-dir=/etcd
    volumes:
      - ./volumes/milvus/etcd:/etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 30s
    networks:
      - agmind-backend

  milvus-init:
    # One-shot: create milvus bucket в существующем MinIO до старта milvus-standalone.
    # Идемпотентно — `mc mb --ignore-existing` не падает если bucket уже есть.
    image: minio/mc:${MC_VERSION:-RELEASE.2025-08-13T08-35-41Z}
    container_name: agmind-milvus-init
    security_opt:
      - no-new-privileges:true
    profiles:
      - milvus
    depends_on:
      minio:
        condition: service_healthy
    entrypoint:
      - /bin/sh
      - -c
      - |
        set -e
        mc alias set local http://minio:9000 "${MINIO_ROOT_USER}" "${MINIO_ROOT_PASSWORD}"
        mc mb --ignore-existing local/milvus-bucket
        echo "milvus-bucket ready"
    networks:
      - agmind-backend

  milvus:
    # security_opt seccomp:unconfined обязателен для Milvus standalone — иначе
    # некоторые syscalls (memfd_create для mmap) блочатся seccomp default profile.
    image: milvusdb/milvus:${MILVUS_VERSION:-v2.6.15}
    container_name: agmind-milvus
    restart: always
    mem_limit: ${MILVUS_MEM_LIMIT:-16g}
    profiles:
      - milvus
    security_opt:
      - no-new-privileges:true
      - seccomp:unconfined
    command: ["milvus", "run", "standalone"]
    environment:
      ETCD_ENDPOINTS: milvus-etcd:2379
      MINIO_ADDRESS: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD}
      MINIO_BUCKET_NAME: milvus-bucket
      MINIO_REGION: us-east-1
      COMMON_STORAGETYPE: minio
    volumes:
      - ./volumes/milvus/data:/var/lib/milvus
    ports:
      # gRPC bound to 127.0.0.1 — внутрисетевой доступ через docker network agmind-backend.
      - "${MILVUS_BIND_ADDR:-127.0.0.1}:${MILVUS_GRPC_PORT:-19530}:19530"
      - "${MILVUS_BIND_ADDR:-127.0.0.1}:${MILVUS_METRICS_PORT:-9091}:9091"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9091/healthz || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 20s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 90s
    depends_on:
      milvus-etcd:
        condition: service_healthy
      milvus-init:
        condition: service_completed_successfully
    networks:
      - agmind-backend

  # ==================== Docling (ETL service, GPU batch inference) ====================
  docling:
    <<: *logging-defaults
    image: ${DOCLING_IMAGE:-quay.io/docling-project/docling-serve-cu130:v1.16.1}
    container_name: agmind-docling
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${DOCLING_MEM_LIMIT:-8g}
    mem_swappiness: 0
    profiles:
      - docling
    ports:
      # Expose UI/Swagger on host so ops can hit /docs without SSRF proxy detour.
      # Default 127.0.0.1 — Docling has no auth, public exposure = LAN GPU-DoS risk.
      - "${DOCLING_BIND_ADDR:-127.0.0.1}:${EXPOSE_DOCLING_PORT:-8765}:8765"
    expose:
      - "8765"
    command: ["docling-serve", "run", "--port", "8765", "--host", "0.0.0.0"]
    environment:
      - NVIDIA_VISIBLE_DEVICES=${NVIDIA_VISIBLE_DEVICES:-all}
      # Without NVIDIA_DRIVER_CAPABILITIES the NVIDIA container toolkit defaults
      # to only 'graphics' — NVML/libcuda are absent and torch.cuda.is_available()
      # returns False. Docling falls back to CPU despite cuda image. vLLM has
      # this same var set to 'compute,utility' and works. Mirror that here.
      - NVIDIA_DRIVER_CAPABILITIES=${NVIDIA_DRIVER_CAPABILITIES:-compute,utility}
      - OCR_LANG=${OCR_LANG:-rus,eng}
      # GPU batch inference — conservative defaults for DGX Spark unified memory.
      # On Spark vLLM holds ~83 GiB of the 124 GiB unified VRAM (gpu_mem_util=0.6).
      # Parallel docling workers × batch_size=64 peaks at ~30-40 GiB → CUDA OOM.
      # Safer: 1 worker, batch=32 → peak ~8-12 GiB, fits alongside vLLM.
      # Tune up only on dedicated-GPU hosts (no shared vLLM), or after profiling.
      - DOCLING_SERVE_LAYOUT_BATCH_SIZE=${DOCLING_SERVE_LAYOUT_BATCH_SIZE:-32}
      - DOCLING_SERVE_OCR_BATCH_SIZE=${DOCLING_SERVE_OCR_BATCH_SIZE:-32}
      - DOCLING_SERVE_TABLE_BATCH_SIZE=${DOCLING_SERVE_TABLE_BATCH_SIZE:-4}
      - DOCLING_SERVE_ENABLE_REMOTE_SERVICES=true
      - DOCLING_SERVE_ENABLE_UI=true
      - DOCLING_SERVE_ALLOW_CUSTOM_OCR_CONFIG=true
      - DOCLING_SERVE_ENG_LOC_NUM_WORKERS=${DOCLING_SERVE_WORKERS:-1}
      - DOCLING_SERVE_MAX_SYNC_WAIT=${DOCLING_SERVE_MAX_SYNC_WAIT:-600}
      - DOCLING_DEVICE=${DOCLING_DEVICE:-cuda}
      - DOCLING_NUM_THREADS=${DOCLING_NUM_THREADS:-12}
      - UVICORN_WORKERS=${DOCLING_UVICORN_WORKERS:-1}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes:
      - agmind_docling_cache:/opt/app-root/src/.cache
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8765/health')\" 2>/dev/null || curl -sf http://localhost:8765/health 2>/dev/null || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 120s
    networks:
      - agmind-backend

  # ==================== LiteLLM (AI Gateway) ====================
  litellm:
    <<: *security-defaults
    image: ghcr.io/berriai/litellm:${LITELLM_VERSION:-v1.83.14-stable.patch.3}
    container_name: agmind-litellm
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${LITELLM_MEM_LIMIT:-1g}
    profiles:
      - litellm
    environment:
      - DATABASE_URL=postgresql://${DB_USERNAME:-postgres}:${DB_PASSWORD:-}@db:5432/litellm
      - LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY:-}
      - LITELLM_LOG_LEVEL=${LOG_LEVEL:-INFO}
    volumes:
      - ./litellm-config.yaml:/app/config.yaml:ro
    command: ["--config", "/app/config.yaml", "--port", "4000", "--num_workers", "2"]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:4000/health/readiness')"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 30s
    depends_on:
      db:
        condition: service_healthy
    networks:
      - agmind-backend

  # ==================== Sandbox (Code Execution) ====================
  # NOTE: sandbox does NOT inherit *security-defaults.
  # no-new-privileges:true blocks cap_add (SYS_ADMIN needed for sandbox namespaces).
  # cap_drop: ALL + cap_add is incompatible with sandbox's chroot/mount operations.
  sandbox:
    image: langgenius/dify-sandbox:${SANDBOX_VERSION:-0.2.15}
    container_name: agmind-sandbox
    restart: always
    mem_limit: ${SANDBOX_MEM_LIMIT:-1g}
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
    cap_add:
      - SETUID
      - SETGID
      - SYS_ADMIN
    environment:
      API_KEY: ${SANDBOX_API_KEY:-dify-sandbox}
      GIN_MODE: release
      WORKER_TIMEOUT: ${SANDBOX_WORKER_TIMEOUT:-15}
      ENABLE_NETWORK: ${SANDBOX_ENABLE_NETWORK:-true}
      HTTP_PROXY: ${SSRF_PROXY_HTTP_URL:-http://ssrf_proxy:3128}
      HTTPS_PROXY: ${SSRF_PROXY_HTTPS_URL:-http://ssrf_proxy:3128}
      SANDBOX_PORT: ${SANDBOX_PORT:-8194}
    volumes:
      - ./volumes/sandbox/conf:/conf:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8194/health 2>/dev/null || wget -qO- http://localhost:8194/health 2>/dev/null || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 10s
    depends_on:
      ssrf_proxy:
        # FIX 2026-05-15: was `service_healthy` but ssrf_proxy is distroless
        # (no shell/utils for CMD-SHELL healthcheck) → healthcheck removed
        # → use service_started. squid binds 3128 within 2-3s of start, so
        # sandbox connecting to it racing is a non-issue in practice.
        condition: service_started
    networks:
      - ssrf-network

  # ==================== SSRF Proxy ====================
  ssrf_proxy:
    <<: *security-defaults
    image: ubuntu/squid:${SQUID_VERSION:-7.2-26.04_edge}
    container_name: agmind-ssrf-proxy
    security_opt:
      - no-new-privileges:true
    restart: always
    mem_limit: ${SSRF_MEM_LIMIT:-256m}
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - NET_RAW
    volumes:
      - ./volumes/ssrf_proxy/squid.conf:/etc/squid/squid.conf:ro
    # FIX 2026-05-15: ubuntu/squid:7.2-26.04_edge is distroless-style
    # (pebble init + squid only — no /usr/bin/head, no /usr/bin/which, no
    # standard PID file → `squid -k check` from CMD-SHELL always exits
    # non-zero even when squid is actively serving). Matches the
    # CLAUDE.md §8 distroless pattern → drop healthcheck, dependents
    # use `service_started` instead of `service_healthy` (sandbox below).
    healthcheck:
      test: ["NONE"]
    networks:
      - ssrf-network
      - agmind-backend

  # ==================== Redis Lock Cleaner (init-container) ====================
  redis-lock-cleaner:
    image: redis:${REDIS_VERSION:-7.4.8-alpine}
    container_name: agmind-redis-lock-cleaner
    security_opt:
      - no-new-privileges:true
    restart: "no"
    mem_limit: 64m
    volumes:
      - ../scripts/redis-lock-cleanup.sh:/cleanup.sh:ro
    environment:
      REDIS_HOST: ${REDIS_HOST:-redis}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
    entrypoint: ["sh", "/cleanup.sh"]
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - agmind-backend

  # ==================== Plugin Daemon ====================
  # Canonical config matching official langgenius/dify docker-compose.
  # Dify manages plugin isolation internally — no cap_drop needed.
  plugin_daemon:
    <<: *logging-defaults
    image: langgenius/dify-plugin-daemon:${PLUGIN_DAEMON_VERSION:-0.6.0-local}
    container_name: agmind-plugin-daemon
    security_opt:
      - no-new-privileges:true
    restart: always
    # Phase 41 calibration (2026-04-18): Dify chat load 8 VU × 2min → 760 msgs,
    # plugin_daemon steady ~2 GiB (cold-start init of all python plugins),
    # chat working-set peak 645 MiB before recreate. No OOM at 3g; 4g gives
    # 2x headroom over steady 2 GiB. Was 10g legacy under old architecture
    # where docling plugin ran inside plugin_daemon (peak 6.5 GiB GPU+CPU).
    # Docling is now standalone (agmind-docling) — plugin_daemon handles only
    # lightweight python plugins (openai_api_compatible, etc.).
    mem_limit: ${PLUGIN_DAEMON_MEM_LIMIT:-4g}
    # Docker embedded DNS (127.0.0.11) дропает большие DNS ответы для CDN
    # (kunlunaq.com / xethub HF LFS — Docker embedded DNS drops large responses).
    # Hardcoded IP — workaround. CDN ротирует IP каждые недели, обновлять снапшотом
    # `dig +short A <host>`. Last verified 2026-04-28.
    # TODO: structural fix — `dns: [1.1.1.1, 8.8.8.8]` для plugin_daemon, тогда
    # extra_hosts не нужен (большие ответы пройдут через upstream resolver).
    extra_hosts:
      - "cdn-lfs-cn-1.modelscope.cn:163.181.225.49"
      - "huggingface.co:52.222.136.92"
      - "cas-bridge.xethub.hf.co:3.164.68.28"
    environment:
      <<: *shared-env
      DB_DATABASE: ${PLUGIN_DB_DATABASE:-dify_plugin}
      DB_SSL_MODE: ${DB_SSL_MODE:-disable}
      SERVER_PORT: ${PLUGIN_DAEMON_PORT:-5002}
      SERVER_KEY: ${PLUGIN_DAEMON_KEY:-}
      MAX_PLUGIN_PACKAGE_SIZE: ${MAX_PLUGIN_PACKAGE_SIZE:-52428800}
      PPROF_ENABLED: ${PLUGIN_PPROF_ENABLED:-false}
      DIFY_INNER_API_URL: ${PLUGIN_DIFY_INNER_API_URL:-http://api:5001}
      DIFY_INNER_API_KEY: ${PLUGIN_INNER_API_KEY:-}
      PLUGIN_REMOTE_INSTALLING_HOST: ${PLUGIN_DEBUGGING_HOST:-0.0.0.0}
      PLUGIN_REMOTE_INSTALLING_PORT: ${PLUGIN_DEBUGGING_PORT:-5003}
      PLUGIN_WORKING_PATH: ${PLUGIN_WORKING_PATH:-/app/storage/cwd}
      FORCE_VERIFYING_SIGNATURE: ${FORCE_VERIFYING_SIGNATURE:-true}
      PYTHON_ENV_INIT_TIMEOUT: ${PLUGIN_PYTHON_ENV_INIT_TIMEOUT:-900}
      PLUGIN_MAX_EXECUTION_TIMEOUT: ${PLUGIN_MAX_EXECUTION_TIMEOUT:-900}
      PLUGIN_STDIO_BUFFER_SIZE: ${PLUGIN_STDIO_BUFFER_SIZE:-1024}
      PLUGIN_STDIO_MAX_BUFFER_SIZE: ${PLUGIN_STDIO_MAX_BUFFER_SIZE:-5242880}
      PIP_MIRROR_URL: ${PIP_MIRROR_URL:-}
      PLUGIN_STORAGE_TYPE: ${PLUGIN_STORAGE_TYPE:-local}
      PLUGIN_STORAGE_LOCAL_ROOT: ${PLUGIN_STORAGE_LOCAL_ROOT:-/app/storage}
      PLUGIN_INSTALLED_PATH: ${PLUGIN_INSTALLED_PATH:-plugin}
      PLUGIN_PACKAGE_CACHE_PATH: ${PLUGIN_PACKAGE_CACHE_PATH:-plugin_packages}
      PLUGIN_MEDIA_CACHE_PATH: ${PLUGIN_MEDIA_CACHE_PATH:-assets}
      SENTRY_ENABLED: ${PLUGIN_SENTRY_ENABLED:-false}
      SENTRY_DSN: ${PLUGIN_SENTRY_DSN:-}
      # Docling s20ss plugin — local VLM picture description (patched tools/utils.py)
      DOCLING_VLM_URL: ${DOCLING_VLM_URL:-http://vllm:8000/v1/chat/completions}
      DOCLING_VLM_MODEL: ${DOCLING_VLM_MODEL:-google/gemma-4-26B-A4B-it}
      DOCLING_VLM_PROMPT: ${DOCLING_VLM_PROMPT:-Опиши кратко что изображено на картинке. 1-2 предложения на русском.}
      DOCLING_VLM_TIMEOUT: ${DOCLING_VLM_TIMEOUT:-120}
      DOCLING_VLM_MAX_TOKENS: ${DOCLING_VLM_MAX_TOKENS:-200}
      DOCLING_VLM_AREA_THRESHOLD: ${DOCLING_VLM_AREA_THRESHOLD:-0.05}
      DOCLING_NUM_THREADS: ${DOCLING_NUM_THREADS:-8}
    volumes:
      - ./volumes/plugin_daemon:/app/storage
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:5002/health/check 2>/dev/null || wget -qO- http://localhost:5002/health/check 2>/dev/null || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 15s
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      redis-lock-cleaner:
        condition: service_completed_successfully
    networks:
      - agmind-backend

  # ==================== Nginx ====================
  nginx:
    <<: *security-defaults
    image: nginx:${NGINX_VERSION:-1.30.0-alpine}
    container_name: agmind-nginx
    security_opt:
      - no-new-privileges:true
    restart: always
    mem_limit: ${NGINX_MEM_LIMIT:-256m}
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - NET_BIND_SERVICE
    read_only: true
    tmpfs:
      - /tmp:size=64m
      - /var/cache/nginx:size=128m
      - /var/run:size=16m
      - /var/log/nginx:size=32m
    ports:
      - "${EXPOSE_NGINX_PORT:-80}:80"
      - "${EXPOSE_NGINX_SSL_PORT:-443}:443"
      - "${EXPOSE_DIFY_PORT:-3000}:3000"
      - "${EXPOSE_LITELLM_PORT:-4001}:4001"
      - "${EXPOSE_DBGPT_PORT:-5670}:5670"
      - "${EXPOSE_NOTEBOOK_PORT:-8502}:8502"
      - "${EXPOSE_NOTEBOOK_API_PORT:-5055}:5055"
      - "${EXPOSE_SEARXNG_PORT:-8888}:8888"
      - "${EXPOSE_CRAWL4AI_PORT:-11235}:11235"
    environment:
      NGINX_SERVER_NAME: ${NGINX_SERVER_NAME:-_}
      NGINX_HTTPS_ENABLED: ${NGINX_HTTPS_ENABLED:-false}
      NGINX_SSL_PORT: ${NGINX_SSL_PORT:-443}
      NGINX_PORT: ${NGINX_PORT:-80}
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      # Mount directory (not single file): health-gen.sh's atomic rename
      # creates new inode; file-mount pins old inode and nginx sees stale data.
      - ./nginx/health/:/etc/nginx/health/:ro
      - ./volumes/certbot/conf:/etc/letsencrypt:ro
      - ./volumes/certbot/www:/var/www/certbot:ro
      - ./volumes/certbot/ssl:/etc/nginx/ssl:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:80/health && curl -sf http://localhost:3000/ || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 10s
    depends_on:
      api:
        condition: service_healthy
      web:
        condition: service_healthy
      plugin_daemon:
        condition: service_healthy
    networks:
      - agmind-frontend
      - agmind-backend

  # ==================== MinIO (Object Storage) ====================
  minio:
    <<: *security-defaults
    image: minio/minio:${MINIO_VERSION:-RELEASE.2025-09-07T16-13-09Z}
    container_name: agmind-minio
    security_opt:
      - no-new-privileges:true
    restart: always
    mem_limit: ${MINIO_MEM_LIMIT:-2g}
    profiles:
      - minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-}
    ports:
      # Default 127.0.0.1 — MinIO Console has admin UI; public exposure = LAN attack surface.
      - "${MINIO_BIND_ADDR:-127.0.0.1}:${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - agmind_minio_data:/data
    healthcheck:
      # MinIO image has mc built-in; "mc ready local" is the official probe.
      # No curl/wget in the minimal image, so don't use HTTP-based checks.
      test: ["CMD", "mc", "ready", "local"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 30s
    networks:
      - agmind-backend

  # ==================== Prometheus (monitoring profile) ====================
  prometheus:
    <<: *security-defaults
    image: prom/prometheus:${PROMETHEUS_VERSION:-v2.54.1}
    container_name: agmind-prometheus
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${PROMETHEUS_MEM_LIMIT:-1g}
    profiles:
      - monitoring
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=15d'
      - '--storage.tsdb.retention.size=2GB'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./monitoring/alert_rules.yml:/etc/prometheus/alert_rules.yml:ro
      - ./monitoring/peer-offline.yml:/etc/prometheus/peer-offline.yml:ro
      - agmind_prometheus_data:/prometheus
    # prom/prometheus = busybox-based (НЕ distroless) — есть /bin/sh + wget,
    # CMD-SHELL healthcheck работает. grafana.depends_on.prometheus использует
    # condition: service_healthy → этот healthcheck обязателен.
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9090/-/healthy || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 15s
    networks:
      - agmind-backend

  # ==================== Alertmanager (monitoring profile) ====================
  alertmanager:
    <<: *security-defaults
    image: prom/alertmanager:${ALERTMANAGER_VERSION:-v0.32.1}
    container_name: agmind-alertmanager
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${ALERTMANAGER_MEM_LIMIT:-256m}
    profiles:
      - monitoring
    volumes:
      - ./monitoring/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
    # prom/alertmanager = busybox-based (НЕ distroless) — CMD-SHELL wget работает.
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9093/-/healthy || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 10s
    networks:
      - agmind-backend

  # ==================== Loki (monitoring profile) ====================
  loki:
    <<: *security-defaults
    image: grafana/loki:${LOKI_VERSION:-3.6.10}
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
    container_name: agmind-loki
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${LOKI_MEM_LIMIT:-1g}
    profiles:
      - monitoring
    volumes:
      - agmind_loki_data:/loki
      - ./monitoring/loki-config.yml:/etc/loki/local-config.yaml:ro
    command: -config.file=/etc/loki/local-config.yaml
    # Loki 3.x uses distroless image (no shell/wget), so no healthcheck
    networks:
      - agmind-backend

  # ==================== Docker Socket Proxy (monitoring profile) ====================
  # Read-only Docker API proxy for cAdvisor + Alloy (SC1 hardening, agmind security audit).
  # Portainer keeps its raw rw socket — proxy cannot satisfy Portainer's write requirements.
  # Tecnativa docker-socket-proxy: arm64-verified (ghcr.io manifest 0.3.0, 2026-05-12).
  docker-socket-proxy:
    <<: *security-defaults
    image: ghcr.io/tecnativa/docker-socket-proxy:${DOCKER_SOCKET_PROXY_VERSION:-0.3.0}
    container_name: agmind-docker-socket-proxy
    restart: unless-stopped
    mem_limit: 64m
    profiles:
      - monitoring
    environment:
      # Read-only allowlist: containers/info/events/images/networks/volumes/ping/version only
      CONTAINERS: 1
      INFO: 1
      EVENTS: 1
      IMAGES: 1
      NETWORKS: 1
      VOLUMES: 1
      PING: 1
      VERSION: 1
      # All write operations denied
      POST: 0
      EXEC: 0
      BUILD: 0
      DELETE: 0
      # Additional operations denied
      AUTH: 0
      COMMIT: 0
      CONFIGS: 0
      CONTAINERS_PRUNE: 0
      DISTRIBUTION: 0
      PLUGIN: 0
      SECRETS: 0
      SESSION: 0
      SWARM: 0
      SYSTEM: 0
      TASKS: 0
      VOLUMES_DELETE: 0
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /usr/local/etc/haproxy
    read_only: true
    # FIX 2026-05-16 (third iteration): read_only:true on this haproxy-based image
    # has TWO writable paths the entrypoint + haproxy need:
    #   1. /usr/local/etc/haproxy/ — entrypoint sed-renders haproxy.cfg from the
    #      image-shipped haproxy.cfg.template. Anonymous volume (copy-up keeps
    #      template, container writes the rendered cfg next to it).
    #   2. /run/haproxy.pid — haproxy master/worker writes its PID file here
    #      per `pidfile /run/haproxy.pid` in the template. tmpfs (in-memory,
    #      ephemeral — PID file gets recreated each container start anyway).
    # Both writable paths declared. If a 4th writable path surfaces, drop
    # `read_only: true` for this service — SC1 cap_drop / no-new-privileges /
    # docker.sock:ro / POST=EXEC=0 already cover 95% of the threat surface;
    # read_only is the cherry on top, not worth chronic whack-a-mole.
    tmpfs:
      - /run:size=16m
    security_opt:
      - no-new-privileges:true
    networks:
      - agmind-backend

  # ==================== Alloy (monitoring profile) ====================
  # Replaces deprecated Promtail (BACKLOG #999.5, migrated 2026-04-25).
  # Reads docker container logs → forwards to Loki via docker-socket-proxy. arm64-verified.
  alloy:
    <<: *security-defaults
    image: grafana/alloy:${ALLOY_VERSION:-v1.16.1}
    container_name: agmind-alloy
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${ALLOY_MEM_LIMIT:-256m}
    profiles:
      - monitoring
    volumes:
      - ./monitoring/alloy-config.river:/etc/alloy/config.river:ro
      - agmind_alloy_data:/var/lib/alloy/data
    command:
      - run
      - /etc/alloy/config.river
      - --storage.path=/var/lib/alloy/data
      - --server.http.listen-addr=0.0.0.0:12345
    # No healthcheck — Alloy distroless image не имеет wget/curl/sh.
    # Distroless — нет /bin/sh (как у redis-exporter, nginx-exporter).
    # Мониторинг через Prometheus `up{job="alloy"}` если targets настроены.
    depends_on:
      loki:
        condition: service_started
      docker-socket-proxy:
        condition: service_started
    networks:
      - agmind-backend

  # ==================== Node Exporter (monitoring profile) ====================
  node-exporter:
    <<: *security-defaults
    image: prom/node-exporter:${NODE_EXPORTER_VERSION:-v1.11.1}
    container_name: agmind-node-exporter
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${NODE_EXPORTER_MEM_LIMIT:-128m}
    profiles:
      - monitoring
    pid: host
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
      - ./monitoring/textfile:/textfile:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--path.rootfs=/rootfs'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
      - '--collector.textfile.directory=/textfile'
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:9100/metrics || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - agmind-backend

  # ==================== cAdvisor (monitoring profile) ====================
  # cAdvisor requires privileged access to read cgroups, /proc, /sys, and
  # Docker storage driver paths.  It is intentionally excluded from
  # x-security-defaults (cap_drop + no-new-privileges would break metrics).
  # See: https://github.com/google/cadvisor/issues/3749
  cadvisor:
    <<: *logging-defaults
    image: gcr.io/cadvisor/cadvisor:${CADVISOR_VERSION:-v0.55.1}
    container_name: agmind-cadvisor
    restart: unless-stopped
    mem_limit: ${CADVISOR_MEM_LIMIT:-512m}
    privileged: true
    profiles:
      - monitoring
    expose:
      - "8080"
    environment:
      # SC1 hardening: route docker queries through read-only proxy (agmind security audit).
      # cAdvisor Docker client respects DOCKER_HOST env (standard Docker SDK behavior).
      DOCKER_HOST: "tcp://docker-socket-proxy:2375"
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
      - /run/containerd/containerd.sock:/run/containerd/containerd.sock:ro
    command:
      # Docker 29.x + containerd snapshotter: need both docker + containerd sockets
      - '--docker_only=true'
      - '--store_container_labels=true'
      - '--housekeeping_interval=30s'
      - '--containerd=/run/containerd/containerd.sock'
      - '--disable_metrics=percpu,sched,tcp,udp,disk,diskIO,hugetlb,referenced_memory'
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/healthz || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 15s
    depends_on:
      docker-socket-proxy:
        condition: service_started
    networks:
      - agmind-backend

  # ==================== Redis Exporter (monitoring profile) ====================
  # Phase 38: exposes redis_* metrics for RedisConnectionError alert.
  redis-exporter:
    <<: *security-defaults
    image: oliver006/redis_exporter:${REDIS_EXPORTER_VERSION:-v1.83.0}
    container_name: agmind-redis-exporter
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${REDIS_EXPORTER_MEM_LIMIT:-64m}
    profiles:
      - monitoring
    environment:
      REDIS_ADDR: "redis://redis:6379"
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
    expose:
      - "9121"
    # No healthcheck: oliver006/redis_exporter is distroless (no /bin/sh), so
    # CMD-SHELL probes fail with exit -1 and container is forever unhealthy.
    # Liveness is monitored via Prometheus `up{job="redis-exporter"}` metric.
    networks:
      - agmind-backend

  # ==================== Postgres Exporter (monitoring profile) ====================
  # Phase 38: exposes pg_* metrics for PostgresConnectionSaturation alert.
  postgres-exporter:
    <<: *security-defaults
    image: prometheuscommunity/postgres-exporter:${POSTGRES_EXPORTER_VERSION:-v0.19.1}
    container_name: agmind-postgres-exporter
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${POSTGRES_EXPORTER_MEM_LIMIT:-128m}
    profiles:
      - monitoring
    environment:
      DATA_SOURCE_NAME: "postgresql://${DB_USERNAME:-postgres}:${DB_PASSWORD:-}@db:5432/${DB_DATABASE:-dify}?sslmode=disable"
    expose:
      - "9187"
    # prometheuscommunity/postgres-exporter = busybox-based (НЕ distroless) —
    # CMD-SHELL wget работает.
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:9187/metrics || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 5s
      retries: 3
      start_period: 15s
    networks:
      - agmind-backend

  # ==================== Nginx Exporter (monitoring profile) ====================
  # Phase 38: exposes nginx_* metrics (connections, requests). Requires
  # stub_status location in nginx.conf restricted to 172.18.0.0/16 (backend net).
  nginx-exporter:
    <<: *security-defaults
    image: nginx/nginx-prometheus-exporter:${NGINX_EXPORTER_VERSION:-1.5.1}
    container_name: agmind-nginx-exporter
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${NGINX_EXPORTER_MEM_LIMIT:-64m}
    profiles:
      - monitoring
    command:
      - '--nginx.scrape-uri=http://nginx:80/stub_status'
    expose:
      - "9113"
    # No healthcheck: nginx-prometheus-exporter is distroless (no /bin/sh), so
    # CMD-SHELL probes fail with exit -1 and container is forever unhealthy.
    # Liveness is monitored via Prometheus `up{job="nginx-exporter"}` metric.
    networks:
      - agmind-backend

  # ==================== Grafana (monitoring profile) ====================
  grafana:
    <<: *security-defaults
    image: grafana/grafana:${GRAFANA_VERSION:-13.0.1}
    container_name: agmind-grafana
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${GRAFANA_MEM_LIMIT:-512m}
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
    profiles:
      - monitoring
    ports:
      - "${GRAFANA_BIND_ADDR:-127.0.0.1}:${GRAFANA_PORT:-3001}:3000"
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      # Phase 44: expose Dify DB creds so provisioned postgres datasource can envsubst
      DB_USERNAME: ${DB_USERNAME:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-}
      DB_DATABASE: ${DB_DATABASE:-dify}
    volumes:
      - agmind_grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 15s
    depends_on:
      prometheus:
        condition: service_healthy
    networks:
      - agmind-frontend
      - agmind-backend

  # ==================== Portainer (portainer profile) ====================
  # ⚠ SECURITY: Portainer requires RW docker.sock = full host control.
  # Mitigations: localhost-only binding, read-only filesystem, no-new-privileges.
  # Set ENABLE_PORTAINER=false to disable entirely if not needed.
  # Portainer keeps rw docker.sock by design (needs full control); audited by
  # 'agmind security audit' (flags HIGH if not behind Authelia). See T-07-08.
  portainer:
    <<: *security-defaults
    image: portainer/portainer-ce:${PORTAINER_VERSION:-2.41.1}
    container_name: agmind-portainer
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${PORTAINER_MEM_LIMIT:-256m}
    profiles:
      - portainer
    # Portainer process runs as uid=0 but needs membership in the host's `docker`
    # group to read /var/run/docker.sock (mode 660 root:docker). Without group_add
    # the local endpoint snapshot fails permanently → master node shows "Down" in
    # UI even though container is healthy. DOCKER_GID auto-detected at install via
    # `stat -c %g /var/run/docker.sock` and written to .env (lib/config.sh).
    group_add:
      - "${DOCKER_GID:-988}"
    # AGENT_SECRET shared с peer agent — без него master подписывает HMAC-запросы
    # дефолтным/случайным ключом, agent отвергает 403 "Invalid request signature",
    # backend translates это в misleading "Agent already paired with another
    # Portainer instance". Без env запроса peer endpoint провалится с этим
    # сообщением. Persistent в /opt/agmind/.secrets/portainer_agent_secret.
    environment:
      AGENT_SECRET: ${PORTAINER_AGENT_SECRET}
    ports:
      - "${PORTAINER_BIND_ADDR:-127.0.0.1}:${PORTAINER_PORT:-9443}:9443"
    read_only: true
    tmpfs:
      - /tmp
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - agmind_portainer_data:/data
    # portainer-ce is built on scratch — no shell, no wget, no curl.
    # In-container healthcheck is impossible. Use NONE and rely on restart policy.
    healthcheck:
      disable: true
    networks:
      - agmind-frontend
      - agmind-backend

  # ==================== Authelia (authelia profile) ====================
  authelia:
    <<: *security-defaults
    image: authelia/authelia:${AUTHELIA_VERSION:-4.39.19}
    container_name: agmind-authelia
    security_opt:
      - no-new-privileges:true
    profiles: ["authelia"]
    restart: always
    mem_limit: ${AUTHELIA_MEM_LIMIT:-256m}
    volumes:
      - ./authelia:/config:ro
    environment:
      TZ: UTC
    healthcheck:
      test: ["CMD", "authelia", "healthcheck"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 10s
    networks:
      - agmind-backend

  # ==================== SearXNG (Search Engine) ====================
  searxng:
    <<: *security-defaults
    image: docker.io/searxng/searxng:${SEARXNG_VERSION:-2026.5.10-df1f24fb7}
    container_name: agmind-searxng
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${SEARXNG_MEM_LIMIT:-256m}
    profiles:
      - searxng
    volumes:
      - ./searxng-settings.yml:/etc/searxng/settings.yml:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- --timeout=5 http://localhost:8080/healthz || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 15s
    networks:
      - agmind-backend

  # ==================== SurrealDB (for Open Notebook) ====================
  surrealdb:
    <<: *security-defaults
    image: docker.io/surrealdb/surrealdb:${SURREALDB_VERSION:-v2.6.5}
    container_name: agmind-surrealdb
    security_opt:
      - no-new-privileges:true
    user: "0:0"
    restart: unless-stopped
    mem_limit: ${SURREALDB_MEM_LIMIT:-256m}
    profiles:
      - notebook
    command: ["start", "--user", "root", "--pass", "${SURREALDB_PASSWORD:-changeme}", "rocksdb:/mydata/database.db"]
    volumes:
      - agmind_surrealdb_data:/mydata
    healthcheck:
      test: ["CMD", "/surreal", "isready"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 10s
    networks:
      - agmind-backend

  # ==================== Open Notebook (Research Assistant) ====================
  open-notebook:
    <<: *security-defaults
    image: docker.io/lfnovo/open_notebook:${OPEN_NOTEBOOK_VERSION:-1.8.5}
    container_name: agmind-notebook
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${OPEN_NOTEBOOK_MEM_LIMIT:-512m}
    profiles:
      - notebook
    environment:
      - OPEN_NOTEBOOK_ENCRYPTION_KEY=${NOTEBOOK_ENCRYPTION_KEY:-}
      - SURREAL_URL=ws://surrealdb:8000/rpc
      - SURREAL_USER=root
      - SURREAL_PASSWORD=${SURREALDB_PASSWORD:-changeme}
      - SURREAL_NAMESPACE=open_notebook
      - SURREAL_DATABASE=open_notebook
      # Frontend (Next.js) runs in user's browser — API_URL must be externally reachable.
      # Port 5055 is published on the host (see ports mapping above) and resolves via mDNS alias.
      - API_URL=${OPEN_NOTEBOOK_API_URL:-http://agmind-notebook.local:5055}
    depends_on:
      surrealdb:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8502/')"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 30s
    networks:
      - agmind-backend

  # ==================== DB-GPT (Data Analysis Agent) ====================
  dbgpt:
    <<: *security-defaults
    image: docker.io/eosphorosai/dbgpt-openai:${DBGPT_VERSION:-v0.8.0}
    container_name: agmind-dbgpt
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${DBGPT_MEM_LIMIT:-1g}
    profiles:
      - dbgpt
    environment:
      - OPENAI_API_KEY=${DBGPT_API_KEY:-${LITELLM_MASTER_KEY:-}}
      - OPENAI_API_BASE=${DBGPT_API_BASE:-http://litellm:4000/v1}
      - SILICONFLOW_API_KEY=unused
      - DBGPT_LANG=ru
    volumes:
      - agmind_dbgpt_data:/app/pilot/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5670/')"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 60s
    networks:
      - agmind-backend

  # ==================== Crawl4AI (Web Crawler API) ====================
  crawl4ai:
    <<: *security-defaults
    image: docker.io/unclecode/crawl4ai:${CRAWL4AI_VERSION:-0.8.6}
    container_name: agmind-crawl4ai
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${CRAWL4AI_MEM_LIMIT:-2g}
    shm_size: '1g'
    profiles:
      - crawl4ai
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:11235/health')"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 45s
    networks:
      - agmind-backend

  # ==================== n8n (Workflow Automation, optional) ====================
  # Opt-in via ENABLE_N8N=true (wizard checklist «Дополнительные сервисы» → n8n,
  # OR named profile `agents` / `full`). Port-exposed 127.0.0.1:5678 (no nginx
  # vhost — like searxng/notebook). Shares agmind-db Postgres (separate database
  # "n8n", auto-created by lib/compose.sh::create_plugin_db). Encryption key
  # generated by lib/config.sh and persisted in .env (rotation = re-import
  # creds in n8n UI).
  n8n:
    <<: *security-defaults
    image: docker.io/n8nio/n8n:${N8N_VERSION:-2.21.0}
    container_name: agmind-n8n
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${N8N_MEM_LIMIT:-1g}
    profiles:
      - n8n
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${N8N_BIND_ADDR:-127.0.0.1}:${EXPOSE_N8N_PORT:-5678}:5678"
    expose:
      - "5678"
    environment:
      DB_TYPE: postgresdb
      DB_POSTGRESDB_HOST: ${DB_HOST:-db}
      DB_POSTGRESDB_PORT: ${DB_PORT:-5432}
      DB_POSTGRESDB_DATABASE: ${N8N_DB_DATABASE:-n8n}
      DB_POSTGRESDB_USER: ${DB_USERNAME:-postgres}
      DB_POSTGRESDB_PASSWORD: ${DB_PASSWORD:-}
      N8N_ENCRYPTION_KEY: ${N8N_ENCRYPTION_KEY:-}
      N8N_HOST: ${N8N_HOST:-localhost}
      N8N_PORT: "5678"
      N8N_PROTOCOL: http
      N8N_LISTEN_ADDRESS: 0.0.0.0
      WEBHOOK_URL: ${N8N_WEBHOOK_URL:-http://localhost:5678/}
      GENERIC_TIMEZONE: ${N8N_TIMEZONE:-${TZ:-UTC}}
      # n8n 2.x: task runners are mandatory (legacy in-process execution removed).
      N8N_RUNNERS_ENABLED: "true"
      N8N_DIAGNOSTICS_ENABLED: "false"
      # Served over plain http on LAN — disable secure cookie or login breaks.
      N8N_SECURE_COOKIE: "false"
      N8N_HIRING_BANNER_ENABLED: "false"
    volumes:
      - agmind_n8n_data:/home/node/.n8n
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- --timeout=5 http://localhost:5678/healthz || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 30s
    networks:
      - agmind-backend

  # ==================== RAGFlow (BACKLOG #999.7) ====================
  # Specialized document parsing + retrieval engine. NOT a Dify replacement —
  # plugged into Dify через plugin witmeng/ragflow-api (manual install после first start).
  # Three isolated containers: ragflow + ragflow_mysql + ragflow_es01.
  # Reuses existing MinIO (own bucket) и Redis (DB=2). vLLM/embed/rerank — через DNS.
  # Image: infiniflow upstream dev-slim-arm64 by digest (NOT community fork — see versions.env).
  ragflow_mysql:
    <<: *security-defaults
    image: docker.io/mysql:${RAGFLOW_MYSQL_VERSION:-8.0.39}
    container_name: agmind-ragflow-mysql
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${RAGFLOW_MYSQL_MEM_LIMIT:-1g}
    profiles:
      - ragflow
    environment:
      MYSQL_ROOT_PASSWORD: ${RAGFLOW_MYSQL_PASSWORD:-}
      MYSQL_DATABASE: rag_flow
    command:
      - --max_connections=1000
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --default-authentication-plugin=mysql_native_password
      - --tls_version=TLSv1.2,TLSv1.3
      - --binlog_expire_logs_seconds=604800
    volumes:
      - agmind_ragflow_mysql_data:/var/lib/mysql
    expose:
      - "3306"
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p\"$$MYSQL_ROOT_PASSWORD\" --silent || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: ${HEALTHCHECK_RETRIES:-5}
      start_period: 30s
    networks:
      - agmind-backend

  ragflow_es01:
    <<: *security-defaults
    image: docker.io/elasticsearch:${RAGFLOW_ES_VERSION:-9.4.0}
    container_name: agmind-ragflow-es
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${RAGFLOW_ES_MEM_LIMIT:-4g}
    profiles:
      - ragflow
    environment:
      - node.name=es01
      - ELASTIC_PASSWORD=${RAGFLOW_ES_PASSWORD:-}
      # IPC_LOCK is in cap_drop default → memlock impossible. Set false explicitly.
      - bootstrap.memory_lock=false
      - discovery.type=single-node
      - xpack.security.enabled=true
      - xpack.security.http.ssl.enabled=false
      - xpack.security.transport.ssl.enabled=false
      # Disk watermarks low to keep ES yellow→green on small dataset volumes.
      - cluster.routing.allocation.disk.watermark.low=5gb
      - cluster.routing.allocation.disk.watermark.high=3gb
      - cluster.routing.allocation.disk.watermark.flood_stage=2gb
      # Heap = half of mem_limit; leave rest for OS file cache.
      - "ES_JAVA_OPTS=-Xms2g -Xmx2g"
    ulimits:
      nofile: { soft: 65536, hard: 65536 }
    volumes:
      - agmind_ragflow_es_data:/usr/share/elasticsearch/data
    expose:
      - "9200"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS -u elastic:\"$$ELASTIC_PASSWORD\" http://localhost:9200/_cluster/health | grep -qE '\"status\":\"(yellow|green)\"' || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: 20
      start_period: 90s
    networks:
      - agmind-backend

  # ==================== RAGFlow ES Prometheus exporter (ragflow profile) ====================
  # Sidecar для метрик Elasticsearch: heap, indices, cluster health, query latency.
  # Distroless image — нет shell, healthcheck via Prometheus `up{job="ragflow-es"}`.
  ragflow_es_exporter:
    <<: *security-defaults
    image: quay.io/prometheuscommunity/elasticsearch-exporter:${ELASTICSEARCH_EXPORTER_VERSION:-v1.9.0}
    container_name: agmind-ragflow-es-exporter
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${ES_EXPORTER_MEM_LIMIT:-128m}
    profiles:
      - ragflow
    environment:
      - ES_USERNAME=elastic
      - ES_PASSWORD=${RAGFLOW_ES_PASSWORD:-}
    command:
      - '--es.uri=http://ragflow_es01:9200'
      - '--es.all'
      - '--es.indices'
      - '--es.indices_settings'
      - '--es.shards'
    expose:
      - "9114"
    depends_on:
      ragflow_es01:
        condition: service_healthy
    networks:
      - agmind-backend

  ragflow:
    <<: *security-defaults
    image: ${RAGFLOW_IMAGE:-ar2r223/ragflow-spark@sha256:4a0eff2da9bf1ca95d20c4345e2217397a6f40e3e9665105db9d68b4998d6dbe}
    container_name: agmind-ragflow
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${RAGFLOW_MEM_LIMIT:-8g}
    profiles:
      - ragflow
    depends_on:
      ragflow_mysql:
        condition: service_healthy
      ragflow_es01:
        condition: service_healthy
    ports:
      # SEC-RAGFLOW-01: default bind 127.0.0.1 — admin-signup race is open
      # until the first user registers, so a LAN-exposed UI lets anyone on
      # the network claim the admin account on a fresh deploy. The wizard
      # flips this to 0.0.0.0 only after explicit user opt-in. Nginx vhost
      # `agmind-rag.local` proxies the local port for normal LAN access.
      - "${RAGFLOW_BIND_ADDR:-127.0.0.1}:${EXPOSE_RAGFLOW_PORT:-9380}:80"
    expose:
      - "9380"   # Main API + /api/v1/dify/retrieval (External KB compatibility) — internal
      - "9381"   # admin
      - "9382"   # MCP server
      - "9384"   # go-http
    environment:
      # === Doc engine ===
      DOC_ENGINE: elasticsearch
      ES_HOST: ragflow_es01
      ES_PORT: 9200
      ELASTIC_PASSWORD: ${RAGFLOW_ES_PASSWORD:-}

      # === MySQL ===
      MYSQL_HOST: ragflow_mysql
      MYSQL_PORT: 3306
      MYSQL_DBNAME: rag_flow
      MYSQL_PASSWORD: ${RAGFLOW_MYSQL_PASSWORD:-}

      # === Shared MinIO bucket (created by lib/config.sh during install) ===
      MINIO_HOST: minio
      MINIO_PORT: 9000
      MINIO_USER: ${RAGFLOW_MINIO_USER:-ragflow}
      MINIO_PASSWORD: ${RAGFLOW_MINIO_PASSWORD:-}

      # === Shared Redis, DB=2 (Dify holds 0,1) ===
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      REDIS_DB: 2

      # === GPU/CPU device для DeepDoc OCR/layout ===
      # DEFAULT cuda — на DGX Spark vLLM работает на peer (LLM_ON_PEER=true), master GPU свободен.
      # Безопасный откат: RAGFLOW_DEVICE=cpu в .env при OOM (dmesg | grep -i 'out of memory').
      # Метрика для решения: nvidia-smi --query-compute-apps=used_memory --format=csv (sum >70 GiB → откат).
      DEVICE: ${RAGFLOW_DEVICE:-cpu}
      TZ: ${TZ:-UTC}
      MAX_CONTENT_LENGTH: 134217728   # 128 MB

      SVR_HTTP_PORT: 9380
      ADMIN_SVR_HTTP_PORT: 9381
      SVR_MCP_PORT: 9382
      GO_HTTP_PORT: 9384

      HF_ENDPOINT: ${HF_ENDPOINT:-https://huggingface.co}

      # NVIDIA caps required when DEVICE=cuda (no-op on cpu mode).
      NVIDIA_VISIBLE_DEVICES: ${NVIDIA_VISIBLE_DEVICES:-all}
      NVIDIA_DRIVER_CAPABILITIES: ${NVIDIA_DRIVER_CAPABILITIES:-compute,utility}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes:
      # service_conf.yaml — pre-rendered by lib/config.sh::_copy_ragflow_templates
      # (entrypoint.sh не делает env substitution; .template обработка наша).
      # Mount target = symlink resolved path: /ragflow/conf/service_conf.yaml
      # → /ragflow/docker/service_conf.yaml (upstream image symlink).
      - ./ragflow/service_conf.yaml:/ragflow/docker/service_conf.yaml:ro
      # nginx config — UPSTREAM RAGFlow Dockerfile НЕ COPY этих files в image
      # (или Hendrik patches убрали COPY). Без mount у nginx остаётся default
      # welcome page, RAGFlow Web UI не serve. Files взяты с upstream v0.24.0.
      - ./ragflow/nginx/ragflow.conf:/etc/nginx/conf.d/ragflow.conf:ro
      - ./ragflow/nginx/proxy.conf:/etc/nginx/proxy.conf:ro
      - ./ragflow/nginx/empty.conf:/etc/nginx/conf.d/default.conf:ro
      - agmind_ragflow_logs:/ragflow/logs
      - agmind_ragflow_history:/ragflow/history_data_agent
    healthcheck:
      # nginx :80 — root возвращает 200 если RAGFlow Web UI loaded.
      # /v1/system/version (port 9380) требует API key → unauth = 401 → -f fails.
      test: ["CMD-SHELL", "curl -fsS http://localhost/ >/dev/null 2>&1 || exit 1"]
      interval: ${HEALTHCHECK_INTERVAL:-30s}
      timeout: 10s
      retries: 20
      start_period: 180s
    networks:
      - agmind-backend

  # ==================== Certbot (VPS profile only) ====================
  certbot:
    <<: *security-defaults
    image: certbot/certbot:${CERTBOT_VERSION:-v5.6.0}
    container_name: agmind-certbot
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    mem_limit: ${CERTBOT_MEM_LIMIT:-128m}
    profiles:
      - vps
    depends_on:
      nginx:
        condition: service_healthy
    volumes:
      - ./volumes/certbot/conf:/etc/letsencrypt
      - ./volumes/certbot/www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    healthcheck:
      disable: true
    networks:
      - agmind-backend

  # ==================== k6 Load Tester (loadtest profile) ====================
  # Phase 40: manual-run only. `agmind loadtest <scenario>` wraps this.
  # Scenarios live in /scripts, results dumped to /results (host-mounted).
  k6:
    <<: *logging-defaults
    image: grafana/k6:${K6_VERSION:-2.0.0}
    container_name: agmind-k6
    security_opt:
      - no-new-privileges:true
    restart: "no"
    mem_limit: ${K6_MEM_LIMIT:-256m}
    profiles:
      - loadtest
    environment:
      K6_OUT: "json=/results/summary.json"
    volumes:
      - ../scripts/loadtest:/scripts:ro
      - ./volumes/loadtest:/results
    # Default entrypoint 'k6' — real command is supplied by `agmind loadtest`:
    #   docker compose run --rm k6 run /scripts/chat-baseline.js
    networks:
      - agmind-backend

networks:
  agmind-frontend:
    driver: bridge
  agmind-backend:
    driver: bridge
  ssrf-network:
    driver: bridge
    internal: true

volumes:
  agmind_openwebui_data:
  agmind_ollama_data:
  agmind_grafana_data:
  agmind_portainer_data:
  agmind_prometheus_data:
  agmind_authelia_data:
  agmind_loki_data:
  agmind_alloy_data:
  agmind_docling_cache:
  agmind_vllm_cache:
  agmind_tei_cache:
  agmind_tei_rerank_cache:
  agmind_vllm_embed_cache:
  agmind_vllm_rerank_cache:
  agmind_surrealdb_data:
  agmind_dbgpt_data:
  agmind_minio_data:
  agmind_ragflow_mysql_data:
  agmind_ragflow_es_data:
  agmind_ragflow_logs:
  agmind_ragflow_history:
  agmind_n8n_data:
